<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.sevenchannel.infrastructure.mapper.ThreadMapper">
	<select id="findById" parameterType="int" resultType="com.example.sevenchannel.domain.model.threads.ThreadEntity">
		SELECT
			id
			,board_id as boardId
			,title
			,user_id as userId
			,post_count as postCount
			,created_at as createdAt
		FROM threads
		WHERE board_id = #{boardId}
	</select>
</mapper>